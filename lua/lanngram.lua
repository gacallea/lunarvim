require('spellsitter').setup {
  -- hl = 'SpellBad, SpellCap, SpellLocal, SpellRare',
  hl = 'SpellBad',
  captures = {'comment'},  -- set to {} to spellcheck everything
  -- captures = {},  -- set to {} to spellcheck everything
}
